package net.darkblade.smop.entity.group;

/** How a group answers one of its members being attacked. */
public enum GroupReaction {
    /** Everyone piles on the attacker. */
    DEFENSIVE,
    /** Everyone scatters. */
    EVASIVE,
    /** Each member is on its own. */
    NEUTRAL
}
